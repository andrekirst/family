# Docker Compose Override for Development
# This file provides alternative network configuration

services:
  # Alternative: Use host network for all services (Linux only)
  # Uncomment the lines below to use host networking instead of bridge
  
  # postgres-keycloak:
  #   network_mode: host
  #   ports: []  # Remove port mapping when using host network
  
  # postgres-app:
  #   network_mode: host
  #   ports: []
  
  # keycloak:
  #   network_mode: host
  #   ports: []
  #   environment:
  #     KC_DB_URL: jdbc:postgresql://localhost:5433/keycloak
  
  # redis:
  #   network_mode: host
  #   ports: []
  
  # kafka:
  #   network_mode: host
  #   ports: []
  #   environment:
  #     KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092,PLAINTEXT_HOST://localhost:9094
  
  # family-api:
  #   network_mode: host
  #   ports: []
  #   environment:
  #     ConnectionStrings__DefaultConnection: "Host=localhost;Database=family;Username=family;Password=family"
  #     Keycloak__Authority: "http://localhost:8080/realms/family"
  #     Cache__RedisConnectionString: "localhost:6379"
  #     Kafka__BootstrapServers: "localhost:9092"

# Alternative network drivers:
# networks:
#   family-network:
#     driver: macvlan  # For direct network access
#     driver_opts:
#       parent: eth0
#     ipam:
#       config:
#         - subnet: 172.20.0.0/16
#           gateway: 172.20.0.1